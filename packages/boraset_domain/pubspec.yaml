name: boraset_domain
description: >-
  Núcleo de domínio do BoraSet — catálogo de exercícios, modelo de sessão,
  motor de decisão adaptativo e progressão de carga. Dart puro: nenhuma
  dependência de Flutter, para que o motor seja testável sem emulador.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.4.0

dev_dependencies:
  test: ^1.25.0
  lints: ^4.0.0
